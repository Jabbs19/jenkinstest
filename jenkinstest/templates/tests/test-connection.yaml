apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "jenkinstest.fullname" . }}-test-connection"
  labels:
{{ include "jenkinstest.labels" . | indent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "jenkinstest.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
